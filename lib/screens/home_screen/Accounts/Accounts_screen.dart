import 'package:flutter/material.dart';

class accounts extends StatelessWidget {
  const accounts({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(body: Container(child: Center(child: Text("1")),));
  }
}