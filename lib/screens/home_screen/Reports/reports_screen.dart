import 'package:flutter/material.dart';

class reports extends StatelessWidget {
  const reports({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(body: Container(child: Center(child: Text("3")),));
  }
}